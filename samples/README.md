# samples

此目录只存放本地私有压力测试副本，推荐放在 `samples/private/` 并保持 Git 忽略。

不要通过链接或脚本直接在任何原始论文目录中构建。原始来源一律视为只读，必须先复制到 `samples/private/` 后再测试；不得把真实论文、导师 Word、未发表数据或出版商受限资源提交到 GitHub。

可公开、可再分发的合成测试语料统一放在 `tests/fixtures/`，并记录来源、作者、许可证和校验和。
